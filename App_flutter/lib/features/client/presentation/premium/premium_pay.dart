import 'package:flutter/material.dart';

class PremiumPayScreen extends StatelessWidget {
  const PremiumPayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Pago premium')),
      body: const Center(
        child: Text('Flujo de pago pendiente de implementación'),
      ),
    );
  }
}
