# Firebase Data Connect generated client

This directory contains generated helpers for the `example` connector. The
artifacts expose prebuilt GraphQL documents that can be imported from either
CommonJS or ES modules.
